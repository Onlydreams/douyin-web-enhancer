# AGENTS.md

本文件适用于整个仓库。目标是把抖音 Web 增强保持为轻量、可直接安装、可验证且在失败时不破坏网站原生行为的 Userscript。

## 项目状态与权威来源

- 当前项目处于设计阶段，尚无可安装脚本。
- 当前技术方案以 `docs/2026-08-13-douyin-userscript-design.md` 为权威来源。
- 正式实现后，仓库根目录的 `douyin-web-enhancer.user.js` 同时作为源代码和可安装产物，不存在构建步骤。
- `README.md` 描述用户可观察的状态、功能和限制；行为或支持范围变化时必须同步更新。
- 未实现或未取得对应证据的能力不得写成“当前可用”或“已经验证”。

## 实现原则

- 保持单文件、零构建、零第三方运行时依赖，除非用户明确批准改变项目形态。
- bootstrap 负责 GM 存储和菜单；页面控制器不得直接读取或写入 GM 配置。
- 页面生命周期收敛到一个深模块，外部 interface 保持为 `start(settings)`、`updateSettings(settings)` 和 `stop()`。
- 页面易变细节集中在内部适配函数中，优先使用 `data-e2e`、`data-danmu-id`、语义属性、DOM 关系和已验证数据字段，不依赖混淆 class。
- MutationObserver 只处理受影响节点，不在回调中反复全量扫描文档。
- 每个 Observer、timer、动画帧、响应包装、媒体门控和事件监听必须有明确停止或恢复路径。
- 不删除、移动或重建抖音管理的 Feed、播放器和弹幕节点。
- 只恢复脚本明确拥有的 DOM 或媒体状态；所有权不确定时不得根据属性值猜测最后写入者。
- 注释解释兼容性原因、时序约束或非显然边界，不复述代码行为。

## 功能契约

### 视频关键词

- 首版只处理首页“推荐”标准视频流。
- 只匹配标题、描述和话题，不匹配作者、互动数字、章节摘要、AI 摘要或页面其他文字。
- 命中后交给统一导航控制器；视频关键词和 BGM 模块不得各自滚动页面。
- `allow`、`block` 和 `bypass` 对当前激活 epoch 都是终局状态，迟到元数据不得突袭翻转。
- 视频/BGM 配置更新不跳过已经稳定播放的当前视频，从下一次激活开始生效。

### 弹幕关键词

- 以 `[data-danmu-id]` 为弹幕所有权单元，只匹配弹幕显示文本。
- 命中后使用脚本专属属性和 CSS 隐藏，不删除节点，不修改抖音弹幕开关。
- 同时处理首批节点、动态新增、文本后填充和节点复用。
- 关闭、清空、根替换或停止时，只恢复脚本自己标记的弹幕。

### BGM 名称

- 只匹配 `music.title` 和 `musicName`。
- 首版只允许卡片 `awemeId/gid` 与元数据 ID 精确关联。
- 禁止用描述、作者、BGM 名称、播放 URL、Feed 顺序或当前缓存唯一性推断视频身份。
- 无 ID、ID 冲突、同 ID 数据不一致或解析失败时必须 fail-open。
- 只观察页面本来就会收到的数据，不调用私有接口，不发送额外网络请求，不声明 `GM_xmlhttpRequest` 或 `@connect`。

## 无感过滤与媒体安全

- “零可听音频”“不超过一个绘制帧”“前台 250ms/100ms”在 Stage 0 完成前都是待验证目标，不是已确认事实。
- `document-start + @sandbox raw` 不能被当作必然早于页面 `fetch`、首帧或首个音频采样。
- 首版默认只考虑带 ownership token 和争用检测的临时 `muted`，不以 `pause()/play()` 作为实现或补救路径。
- 不能用“属性值仍等于脚本写入值”证明媒体所有权。
- 文档进入后台时立即使当前 epoch 失效并 fail-open；后台标签页不维持待判定空白或临时静音。
- Feed 根替换的清理顺序固定为：先递增 generation 并 retire 旧 epoch，再取消任务、断开观察和恢复可安全恢复的状态。
- 所有异步入口在产生副作用前必须重新校验 generation、epoch 和 retired 状态。

## 隐私与安全

- 关键词只保存在 GM 本地存储，不写入页面 DOM，不发送到网络。
- 不读取 Cookie、Local Storage、浏览历史或额外账号数据。
- 元数据缓存只保留完成匹配所需的最小 DTO，不保留认证字段、完整响应或视频 URL。
- 不点击“不感兴趣”、关注、点赞、评论或其他会改变账号状态的控件。
- 日志不得输出完整关键词、认证字段、账号标识、视频 URL、完整请求或响应。
- 公开提交和文档必须检查本机路径、Token、账号信息和匿名化 fixture。

## 测试与证据

- 使用 Node 18+ 内置 `node:test`，不为测试引入第三方依赖。
- 纯匹配、状态机、元数据提取和关联使用纯函数或 runtime Adapter 测试。
- 控制器测试必须覆盖 generation/epoch 失效、根替换、迟到回调、配置 revision、后台切换、连续跳过熔断、媒体争用和完整清理。
- 传输包装测试必须验证原调用的参数、`this`、Promise/Response 语义不变，解析 clone 不消费页面响应，卸载不覆盖后来者。
- 非平凡分支、循环、解析、媒体状态、异步和 IO 逻辑必须有可运行回归测试。
- 本地测试不能证明 Tampermonkey 注入时序、真实 DOM、首帧、音频输出或浏览器自动播放行为。

证据必须分层报告：

1. 源码语法检查；
2. Node 单元和控制器测试；
3. 代理在当前浏览器中的真实 DOM、网络、录屏和音频验证；
4. 用户手工验收；
5. 尚未验证的浏览器和页面。

旧脚本版本、Node 模拟结果、代理浏览器结果和用户人工确认不能互相替代。

## 最低检查

正式脚本存在后，每次交付至少运行：

```powershell
node --check .\douyin-web-enhancer.user.js
node --test
git diff --check
```

页面选择器、动态加载、媒体、计时、路由或网络观察发生变化时，还必须完成对应的 Tampermonkey 实页验证。无法验证时明确列出缺口，不能把源码测试表述为功能通过。

## 版本、许可与 Git

- 项目使用 MIT License。
- 用户可见行为变化时同步更新 Userscript 元数据和 README。
- 未进入正式发布流程时，不为普通开发改动自动提升版本。
- 修改前检查工作树和当前分支，只处理本次任务范围内文件。
- 未经用户明确要求，不提交、不推送、不创建发布或 PR。
- 用户明确要求提交时，只暂存目标文件；提交前完成格式、敏感信息和验证检查。
- 未经用户明确指令，不修改本文件或其他项目规则文件。
