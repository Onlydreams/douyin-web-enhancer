# 抖音 Web 增强

一个单文件、零构建、零第三方运行时依赖的抖音 Web 端 Tampermonkey Userscript。

## 当前状态

项目当前处于 Stage 5 发布候选收敛，脚本提供视频关键词、BGM 名称和弹幕关键词过滤。当前仓库已经完成：

- 抖音 Web 首页“推荐”标准视频流的真实页面结构调研；
- 视频关键词、弹幕关键词和 BGM 名称过滤的可行性分析；
- 无感过滤、SPA 生命周期、BGM 数据关联和故障降级设计；
- 一轮独立对抗式技术方案审查及修订；
- Stage 0 真实页面证据采集与收敛：活动卡片、三卡虚拟窗口、视频文本边界、弹幕结构、Tampermonkey 注入、XHR Feed 和 BGM 字段关联；
- Stage 0 诊断探针的 17 项离线安全回归，以及 `0.2.1-test` Chrome + Tampermonkey 窄范围冒烟；
- Stage 1 正式单文件骨架、纯关键词规则、六个 GM 存储键和动态菜单。
- Stage 2 可编程下一条能力闸门：Chrome + Tampermonkey 连续 3/3 次确认只点击一次且活动视频 ID 改变；
- Stage 2 视频控制器：推荐流生命周期、视频文本判定、视觉门控、下一条确认/重试、配置 epoch 和连续跳过熔断。
- Stage 3 弹幕控制器：独立于推荐流的页面级观察、首批与动态节点、文本后填充、节点复用、100ms fail-open 和完整恢复。
- Stage 4 BGM 数据层：新增 QuickPlayer script 的有界增量解析、最小 DTO、精确 ID 关联、有界缓存和统一导航决策；`0.0.1` 使用有单飞背压的 XHR/fetch Feed 响应观察。

完整方案见：

- [抖音 Web 增强 Userscript 技术方案](./docs/2026-08-13-douyin-userscript-design.md)
- [Stage 0 真实页面证据报告](./docs/2026-08-13-stage-0-evidence.md)
- [Stage 1 配置骨架验收报告](./docs/2026-08-13-stage-1-evidence.md)
- [Stage 2 视频关键词与导航证据报告](./docs/2026-08-13-stage-2-evidence.md)
- [Stage 3 弹幕关键词证据报告](./docs/2026-08-13-stage-3-evidence.md)
- [Stage 4 BGM 元数据证据报告](./docs/2026-08-13-stage-4-evidence.md)
- [Stage 5 发布候选验收报告](./docs/2026-08-13-stage-5-evidence.md)

根目录的 [`douyin-web-enhancer.user.js`](./douyin-web-enhancer.user.js) 当前待发布版本为 `0.0.1`。此前所有开发、探针和实测版本统一以 `-test` 后缀记录；`0.0.1` 的代码行为继承已完成验证的 `1.0.10-test`。`1.0.7-test` 与 `1.0.8-test` 分别修复历史 script 同步全扫和 DTO 高扇出预算后，BGM 场景仍会令 Chrome 卡死；`1.0.9-test` 曾为止损而停用 Feed 传输，造成 BGM 覆盖率实质下降；`1.0.10-test` 在继续禁止历史 script 同步全扫的同时恢复 XHR/fetch Feed 数据源，并把正文克隆、解码和解析收敛为全传输单飞任务，重叠响应直接 fail-open，解析延后到独立任务。当前 Chrome + Tampermonkey 已通过 BGM 真实命中、仅 BGM 5 次 SPA 往返、三模块组合人工回归，以及约 15.6 分钟、143 次切换、3 次 SPA 往返的代理生命周期回归。独立性能 trace、Network、后台、系统音频和高帧率零帧验收仍未完成，媒体写入仍未启用。`tools/stage0-probe.user.js` 与 `tools/stage2-navigation-probe.user.js` 都是显式启用的诊断工具，不是产品脚本。

Stage 1 菜单为三类功能各提供一个状态项和一个编辑项。状态动态显示“未配置”“已开启（N 个词）”或“已关闭（已保存 N 个词）”；`N` 是 NFKC 规范化、去空和去重后的有效词数，菜单不显示原始关键词。

## 安装与更新

1. 安装 Chrome 和 Tampermonkey；当前实页证据来自 Chrome + Tampermonkey，其他浏览器尚未完成兼容性验收。
2. 在 Tampermonkey 中新建脚本。
3. 用根目录 [`douyin-web-enhancer.user.js`](./douyin-web-enhancer.user.js) 的完整内容替换编辑器内容并保存。
4. 打开或刷新 `https://www.douyin.com/?recommend=1`。

更新时重复第 3 步并保存，然后刷新抖音页面。不要安装 `tools/` 下的诊断探针作为日常产品脚本。

版本编号从测试记录迁移到正式候选时，`0.0.1` 在版本排序上低于此前的 `1.0.10-test`，不能依赖自动更新。当前 Chrome 测试安装已于 2026-08-20 在 Tampermonkey 编辑器中手工全文替换并保存为 `0.0.1`，现有 GM 配置得到保留；其他仍安装测试版的环境也需要完成一次相同迁移。此后版本按 `0.0.x` 正常递增。

## 配置与使用

打开 Tampermonkey 扩展菜单，在“抖音 Web 增强”下分别配置：

- “编辑视频屏蔽词”：用于推荐流标题、描述和话题；
- “编辑 BGM 屏蔽词”：同时匹配原声音轨名和平台明确关联歌曲名；
- “编辑弹幕屏蔽词”：用于单个视频内的弹幕显示文本。

多个关键词使用竖线 `|` 分隔。状态菜单可单独开启或关闭每类规则；关闭后保留已保存关键词，清空编辑框则删除该类关键词。视频/BGM 配置变更不突袭当前已稳定播放的视频，从下一次激活开始生效；弹幕配置对当前页面立即重判。

## 功能

### 视频关键词屏蔽

根据推荐视频的标题、描述和话题匹配本地关键词。命中后保持视觉门控，并通过已在 Chrome + Tampermonkey 3/3 验证的语义化下一条控件进入后续内容；活动视频 ID 未变化时最多重试一次，随后放行并提示。15 秒内连续确认跳过 12 条会触发熔断。

### 弹幕关键词屏蔽

在任意视频页面的弹幕首次绘制前匹配显示文本，只隐藏命中的弹幕，不删除节点，也不改变抖音原生弹幕开关；该功能不依赖推荐流或自动下一条能力。

### BGM 名称屏蔽

对脚本运行期间新增的 QuickPlayer script 做有界静态解析，并只读观察页面本来就会接收的同源 Feed 响应，同时匹配原声条目的 `music.title/musicName` 和平台明确关联歌曲的 `relatedMusicAnchor.extra.title`。只允许通过 `awemeId/gid` 精确关联视频；无法可靠关联、解析超限或已有一个响应正文正在处理时均放行。页面已有 script 不做同步全扫；Feed 正文处理全局最多一个进行中任务，解析延后执行，不发送额外请求。

## 设计原则

- 单文件 Userscript，同时作为源代码和安装产物；
- 零构建、零第三方运行时依赖；
- 关键词仅保存在 GM 本地存储中，不写入页面 DOM，不发送到网络；
- 不调用抖音私有接口，不发送额外网络请求；
- 不点击“不感兴趣”，不修改账号推荐画像；
- 页面或数据结构不确定时 fail-open，优先保持抖音原生功能可用；
- 站点易变的 DOM 和数据细节集中在内部适配函数中；
- 源码测试、代理真实浏览器验证和用户手工验收分别记录，不互相替代。

## 首版范围

视频关键词与 BGM 关键词的自动跳过当前只支持：

- `https://www.douyin.com/` 首页推荐流；
- `https://www.douyin.com/?recommend=1` 推荐流；
- 标准视频卡片。

弹幕关键词是独立的单视频内显示过滤，只要页面使用可识别的 `[data-danmu-id]` 弹幕节点即可工作，不受上述推荐流范围限制。

首版不承诺支持直播、图文、短剧、放映厅、搜索结果、个人主页、评论区、广告或游戏推广卡片，也不承诺兼容 Firefox、Violentmonkey 和 Safari。

## 无感过滤边界

“无感”的方向是：尽量让命中视频不产生可听音频、不进入稳定画面，让命中弹幕不先显示再消失，并避免连续命中的预加载视频逐条闪现。

Stage 0 已确认页面注入、XHR Feed、BGM 字段关联和媒体/弹幕结构；同时观察到首个视频帧早于现有标准活动卡片信号，因此首版不承诺数学意义的绝对零帧或零音频。250ms 视频/BGM 门控和 100ms 弹幕门控只是可见前台标签页中的测量目标，不是后台标签页或所有浏览器状态下的绝对保证。

可靠的可编程导航已经通过 Stage 2 实页能力闸门；`0.4.8-test` 的双向导航和连续命中已通过用户手工验收。临时静音争用与恢复、高帧率录屏和系统音频采样仍待验证；当前候选版不写 `muted`，也不调用 `pause()/play()`。任一剩余能力未通过时，项目会禁用对应动作或降低承诺。

## 开发阶段

1. Stage 0（已完成）：固化真实 DOM、网络、注入和媒体/弹幕结构证据，明确未闭环项归属。
2. Stage 1（已完成）：已建立并验收单文件脚本元数据、关键词规则、GM 存储和动态菜单。
3. Stage 2（已收敛）：导航闸门、视频预判、视觉门控、双向过渡确认、连续命中和熔断已实现；`0.4.8-test` 连续命中实页验收通过。媒体写入仍禁用。
4. Stage 3（已完成）：弹幕首绘过滤、文本后填充、节点复用和恢复已实现；正式 `0.3.1-test` 已通过推荐页与独立视频页实页验收。`0.4.9-test` 取消周期性全页漏事件补扫，依赖增量 Observer，健康检查仅核对路由和根身份。
5. Stage 4（已实现）：原声/关联歌曲的安全解析、精确关联和统一导航保留；`0.0.1` 沿用 `1.0.10-test` 已验证的单飞 Feed 传输观察，继续禁用历史 script 同步全扫。
6. Stage 5（发布候选）：`0.0.1` 继承 `1.0.10-test` 已通过的离线红灯、BGM 真实命中、仅 BGM 与三模块组合实页回归，以及约 15.6 分钟推荐流和 3 次 SPA 往返。Network、独立性能、后台和媒体验收仍未闭环。

## 隐私、权限与停用

- 只申请 `GM_getValue`、`GM_setValue`、`GM_registerMenuCommand` 和 `GM_unregisterMenuCommand`；
- 关键词仅保存在 Tampermonkey 的 GM 本地存储中，不写入页面 DOM，不发送到网络；
- 不读取 Cookie、Local Storage、浏览历史或额外账号数据；
- 不点击“不感兴趣”、关注、点赞、评论等会改变账号状态的控件；
- BGM 只解析页面新增 QuickPlayer script 和页面本来就会接收的 allowlist Feed 响应，不调用私有接口，不发送额外请求；传输观察不修改请求或页面原响应，重叠正文处理会 fail-open；
- 停用时可在 Tampermonkey 管理面板关闭脚本并刷新抖音；卸载时直接删除该脚本。脚本停止、关闭规则或离开支持页面时只恢复自己拥有的页面状态。

## 开发与验证

项目没有构建步骤或依赖安装命令。当前最低验证命令为：

```powershell
node --check .\douyin-web-enhancer.user.js
node --check .\tools\stage0-probe.user.js
node --check .\tools\stage2-navigation-probe.user.js
node --test
git diff --check
```

目录结构：

```text
.
├── AGENTS.md
├── LICENSE
├── README.md
├── douyin-web-enhancer.user.js
├── docs/
├── test/
└── tools/
```

`douyin-web-enhancer.user.js` 同时是源代码和可安装产物；`tools/` 只保存诊断探针，不属于日常安装内容。

Stage 1 已在 Chrome + Tampermonkey 中完成六个菜单、持久化和普通页面惰性验收。Stage 2 导航探针已完成 3/3 实页确认；正式控制器的向下、向上和连续命中链已完成现场诊断，`0.4.8-test` 由用户确认连续命中恢复正常。Stage 3 弹幕控制器已完成源码、Node 回归以及推荐页与独立视频页实页验收。历史 Stage 4 曾确认 BGM 命中与自动跳过，但旧传输观察路径在后续 SPA 单变量测试中导致 Chrome 卡死；`1.0.10-test` 已离线复现重叠响应的无背压正文克隆并改为单飞、延后解析，随后通过真实 BGM 命中、仅 BGM 与三模块组合回归，以及约 15.6 分钟推荐流和 3 次 SPA 往返；待发布 `0.0.1` 沿用相同实现。系统音频、高帧率零帧和媒体写入仍未验证或启用。

## 许可

本项目采用 [MIT License](./LICENSE)。
