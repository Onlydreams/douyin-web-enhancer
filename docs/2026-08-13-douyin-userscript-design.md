# 抖音 Web 增强 Userscript 技术方案

> 状态：`0.0.3` 发布候选；本轮弹幕门控、跨实例背压与卡片清理修复已通过 Node 回归，Tampermonkey 实页待验收。此前 `0.0.2` / `1.0.10-test` 的实页记录保留为历史证据。
> 日期：2026-08-13
> 目标产物：单文件 Tampermonkey Userscript
> 首版范围：视频/BGM 自动跳过限首页“推荐”标准视频流；弹幕过滤覆盖可识别弹幕的视频页面
> 本次修订：完成 Stage 0 证据收敛，按已确认、能力闸门和人工验收重新划定“无感过滤”边界

> 对抗式审查处置：2026-08-13 完成独立审查；采纳 ID-only BGM 关联、媒体争用检测、先失效后清理、终局决策语义、前台计时和注入证据分层。`pause()/play()` 不进入首版默认路径。

## 1. 决策摘要

本项目参考 `zhihu-web-enhancer` 和 `xiaohongshu-web-enhancer` 的交付形式，采用：

- 单文件 Userscript，同时作为源码和可安装产物；
- 零构建、零第三方运行时依赖；
- Node 内置测试运行器；
- Tampermonkey 菜单和 GM 本地存储；
- 真实 DOM 优先、站点易变细节集中隔离；
- 只改变本地显示和播放流程，不点击“不感兴趣”，不提交推荐反馈；
- 不调用抖音私有接口，不发送额外网络请求；
- 读取页面本来就会接收的 Feed 数据，以支持 BGM 名称识别；
- 在视频或弹幕绘制前完成可判定内容的过滤；需要等待 BGM 时使用有界逐卡门控和临时音频抑制。

三个功能分别维护独立开关和独立词表：

1. 视频关键词命中后自动进入下一条；
2. 弹幕关键词命中后只隐藏对应弹幕；
3. BGM 名称命中后自动进入下一条。

视频关键词与 BGM 名称仅是统一视频决策模块的两种输入，命中后的门控、导航、确认、恢复与熔断行为完全一致；弹幕过滤属于独立的页面级显示模块，不触发导航，也不依赖推荐流生命周期。

关键词首版只支持大小写不敏感的子串匹配，不支持正则表达式、通配符或布尔表达式。

“无感”在本方案中的工程方向是：在可见前台标签页中，尽量让命中视频不产生可听音频、不进入稳定画面，让命中弹幕不先显示再消失；已预加载并可判定的连续命中卡片直接越过。无法判定时目标门控上限为 250ms，随后 fail-open。Stage 0 已证明现有活动卡片信号晚于首个绘制帧，因此首版不承诺数学意义的绝对零音频或零帧；实际效果必须按源码、浏览器测量和人工验收分层报告。

## 2. 参考项目中复用与不复用的设计

### 2.1 复用的设计

从两个参考项目复用以下约定：

- 保持单文件、直接安装、无构建步骤；
- bootstrap 负责 GM 存储和菜单，页面控制器不直接读取 GM 配置；
- 关键词在配置变化时统一规范化、去空、去重和编译，不在每个节点上反复读取存储；
- 菜单动态显示“未配置 / 已开启 / 已关闭（已保存）”以及有效词数；
- MutationObserver 只收集受影响节点，不在每次回调中全量扫描整个文档；
- 页面根替换、SPA 路由变化、关闭功能和控制器停止都有明确清理路径；
- 只恢复脚本自己拥有的 DOM 状态；
- 页面选择器、动态加载和真实交互必须在最新 Tampermonkey 副本上实测，Node 测试不能代替实页验证。

### 2.2 不直接复用的设计

以下实现与抖音页面模型不匹配，不应照搬：

- 知乎脚本中分散在多个页面分支里的独立 Observer；抖音首版应集中到一个生命周期控制器；
- 小红书瀑布流的布局接管、列识别和 transaction；抖音只切换视频，不需要重排 Feed；
- 知乎通过 `display:none` 隐藏完整内容卡片；抖音视频命中后应切换下一条，而不是从虚拟列表中删除卡片；
- 依赖混淆 CSS 类名的定位；抖音适配优先使用 `data-e2e`、语义属性、DOM 关系和数据字段。

## 3. 当前实页证据

2026-08-13 在登录状态下检查 `https://www.douyin.com/?recommend=1`。完整匿名证据见 `docs/2026-08-13-stage-0-evidence.md`，当前确认：

| 对象 | 当前可用信号 | 结论 |
| --- | --- | --- |
| Feed 根 | 唯一 `[data-e2e="slideList"][data-active="true"]` | 作为当前推荐流发现锚点 |
| 视频卡片 | 唯一 `[data-e2e="feed-active-video"][data-e2e-vid]`，相邻项为 `feed-video` | 活动卡片主判据；稳定三卡窗口中活动索引为 1 |
| 视频描述 | `[data-e2e="video-desc"]` | 当前包含标题、描述和话题文本 |
| 当前视频 | 唯一 `feed-active-video` 及其 19 位 `data-e2e-vid` | 不再以几何、可见面积或 `paused` 作为主判据 |
| 弹幕输入 | `[data-e2e="danmaku-container"]` | 可定位当前播放器弹幕区域 |
| 弹幕节点 | `[data-danmu-id]`，外层存在 `.danmu` | 弹幕是 DOM，不是 Canvas，可逐条过滤 |
| 视频标识 | 后代链接中的 `aweme_id` / `gid`（部分卡片） | 可作为精确关联的第一优先级 |
| BGM 数据 | QuickPlayer `awemeInfo.awemeId` 与 `music.title/musicName`、`relatedMusicAnchor.extra.title` 同对象；XHR Feed 中 `aweme_id + music.title` 同对象 | 原声和平台明确关联歌曲均可进入最小 DTO，并只按 ID 精确关联 |
| 后续 Feed | `GET /aweme/v1/web/tab/feed/`，主实测传输为 XHR | XHR Adapter 为主路径，`fetch` 保留兼容 seam |
| 注入 seam | Chrome + Tampermonkey 中 GM API 与 `unsafeWindow` 页面包装共存 | 当前组合可观察页面 XHR/fetch，但不推出绝对早于页面全部脚本 |
| 媒体时序 | 首帧约 2055ms，首个标准活动卡片约 3414ms | 当前活动卡片选择器不能单独承担首帧前门控 |
| 下一条行为 | Edge 真实 `ArrowDown` 曾 12/12；Chrome 键盘控制曾不稳定；Stage 2 语义化下一条 Tampermonkey 探针 3/3 | 活动 ID 是确认信号；`[data-e2e="video-switch-next-arrow"] .click()` 已通过 Stage 2 能力闸门 |

页面还存在大量混淆 class，例如弹幕和播放器内部 class。这些 class 只能作为当前结构的辅助信号，不能成为核心接口。

## 4. 目标与非目标

### 4.1 首版目标

- 支持抖音首页推荐流的首屏、预加载卡片和继续下滑；
- 视频关键词匹配标题、描述和话题，不匹配作者、评论、章节摘要或页面其他文字；
- 弹幕关键词只匹配每条弹幕的显示文本；
- BGM 关键词只匹配原声 `music.title/musicName` 和同一视频对象的平台关联歌曲 `relatedMusicAnchor.extra.title`，不把作者字段单独加入匹配范围；
- 三组规则可以独立开启、关闭、编辑和清空；
- 弹幕配置变化立即作用于当前弹幕和后续弹幕；视频/BGM 配置不突袭跳过已稳定播放的当前视频，从下一次激活开始生效；
- 对预加载视频提前形成决策，使已知命中内容不成为稳定活动卡片；
- 媒体安全闸门通过后，以命中视频不产生可听音频为验收目标；命中弹幕以不出现“先显示后隐藏”为验收目标；
- BGM 元数据尚未就绪时，允许对可见前台中已可靠识别的候选卡片进行目标最长 250ms 的视觉门控；媒体门控只有通过 Stage 2 所有权与恢复闸门后才启用；
- 对无法识别或无法可靠关联的数据 fail-open，即放行而不是猜测；
- 所有观察器、计时器、响应拦截包装和 DOM 所有权都可停止或恢复；
- 不在 DOM、日志或网络中暴露用户的完整关键词列表。

### 4.2 首版非目标

- 直播卡片；
- 图文、短剧、放映厅、搜索结果、个人主页和评论区；
- 未经实测的“精选”“关注”“朋友”等频道；
- 广告卡片和游戏推广卡片的专门处理；
- 点击“不感兴趣”或修改账号推荐画像；
- 读取历史记录、Local Storage、Cookie 或额外账号数据；
- 正则表达式、词组逻辑、云同步、命中统计或导入导出；
- 在所有浏览器调度、网络延迟、特殊卡片和未来页面实现中保证绝对零帧；
- Firefox、Violentmonkey 和 Safari 的首版兼容性承诺。

后续页面只有在取得真实 DOM 和数据证据后，才增加新的页面 Adapter；不能仅因选择器看起来相似就扩大支持范围。

## 5. 行为契约

### 5.1 关键词语义

三组关键词采用同一纯匹配模块：

1. 输入格式为 `关键词A|关键词B|关键词C`；
2. 对每项执行 Unicode NFKC 规范化、连续空白压缩和首尾去空；
3. 使用 `toLowerCase()` 做大小写不敏感匹配；
4. 按规范化结果去重；
5. 空项忽略；
6. 使用普通子串匹配；
7. Emoji 保持原样，不主动删除变体选择符；
8. 关键词数量以规范化、去空、去重后的结果为准。

不把词表写入页面 DOM。菜单只显示有效词数，不展示具体词。

### 5.2 视频关键词

首版只读取当前活动卡片 `[data-e2e="video-desc"]` 的可见文本及其话题链接。`itemTitle`、`desc`、`caption` 是后续可评估的数据源候选，不进入首版传输 DTO；在没有建立作者隔离和迟到字段终局语义的实页证据前，不从 Feed 响应扩展视频文本匹配。

作者名、互动数字、章节摘要、AI 摘要、游戏推广文本和页面其他卡片不得进入匹配输入。

一旦任一视频关键词命中，不需要等待 BGM 元数据，立即请求切换下一条。

### 5.3 弹幕关键词

- 观察范围限定到当前活动视频卡片内的弹幕层；
- 以 `[data-danmu-id]` 为弹幕所有权单元；
- 新节点、文本后填充和节点复用都要重新判断；
- 命中后添加脚本专属属性，由专属 CSS 隐藏；
- 未命中或配置关闭时，只移除脚本添加的属性；
- 不删除弹幕节点，不修改抖音弹幕开关，不影响用户发送弹幕；
- 点赞数、菜单文字等辅助文本应在实测后从弹幕正文提取函数中排除；无法可靠分离时，以完整渲染文本匹配并记录这一边界。

### 5.4 BGM 名称

BGM 过滤必须同时满足：

1. 已获得当前视频的原声 `music.title/musicName`，或同一视频对象内平台明确关联歌曲的 `relatedMusicAnchor.extra.title`；
2. 元数据能可靠关联到当前卡片；
3. 任一名称命中同一套 BGM 词表。

左侧歌曲识别/关联名称和右侧“创作的原声”属于不同数据字段，但对 BGM 过滤都有效。关联歌曲的 `extra` 必须是同一 `awemeInfo` 内有界 JSON 字符串，只提取其 `title`；不保留作者、封面、落地页或歌曲 ID。

关联只有一条允许路径：卡片取得的 `aweme_id` / `gid` 必须与元数据 `awemeId` 精确相等。无 ID、ID 冲突或同一 ID 出现不一致元数据时一律放行并停用该记录的 BGM 判定。

首版明确禁止：

- 使用描述 + 作者做 fallback；
- 使用当前缓存中的“唯一候选”推断身份；
- 按 Feed 数组或 DOM 顺序猜测当前视频；
- 仅凭 BGM 名称、作者或播放 URL 反推视频身份。

只有 BGM 规则开启而元数据暂未到达时，才进入有界等待：

- 预加载且尚未接近激活的卡片可以继续等待元数据，不启动可见门控期限；
- 可见前台标签页中，卡片成为下一激活候选或当前活动卡片时，启动不可重置的目标 250ms 决策期限；
- 等待期间保持已可靠识别卡片的视觉门控；只有媒体安全闸门通过后才临时抑制其音频；
- 期限内取得可靠元数据后立即形成 `allow` 或 `block`；
- 达到期限仍无法可靠关联则形成当前激活周期的 `bypass` 并放行；
- `bypass` 后到达的迟到元数据不得让正在观看的视频突然被跳过；只有该卡片离开并在新的激活周期返回时才允许重新判定；
- 不无限轮询，也不因 BGM 缺失持续保持空白。

### 5.5 自动下一条

切换动作由一个内部函数集中处理，不允许视频关键词和 BGM 模块各自滚动页面。

该能力的前置闸门已于 2026-08-13 通过：Chrome + Tampermonkey 探针连续三次调用唯一 `[data-e2e="video-switch-next-arrow"]`，每次 `clickCount: 1`，并确认唯一活动 `data-e2e-vid` 改变；测试参数在动作前被消费，未执行账号状态操作。匿名证据见 `test/fixtures/stage2-navigation-chrome.json`。

闸门通过后的流程：

1. 为当前卡片生成稳定决策键，优先使用 `awemeId`；
2. 将命中卡片保持在视觉门控状态；媒体安全闸门也通过时，才按媒体所有权协议临时静音当前卡片内正在播放的视频；
3. 记录当前唯一活动 `data-e2e-vid`，并确认语义化下一条控件唯一且可用；虚拟列表的已加载卡数和 DOM 顺序不作为导航能力门控；
4. 使用 Tampermonkey 探针实测通过的唯一可编程方式请求进入下一条；真实 `ArrowDown` 只作为人工基线，不视为脚本能力；
5. 等待“活动卡片已经改变”的确认信号；
6. 确认前禁止发起第二次切换；
7. 超时仍未改变时最多重试一次，之后 fail-open 并显示简短提示，避免连续误跳。

Stage 2 已验证页面存在可调用且不会改变账号状态的语义化下一条控件。正式实现不使用合成 `KeyboardEvent`、滚动、点击下一卡或其他并行 fallback；只保留该 `.click()` 主路径和一次带活动 ID 确认的重试。

实页检查已否定“点击下一卡”和“标准滚动容器”两条路径。`nextCard.click()` 不改变活动 ID，Feed 由虚拟列表 `transform` 驱动，不能把浏览器自动滚入视图误判为站点切换。真实键盘输入在 Edge 基线中曾稳定，但 Chrome 控制输入不稳定；它既不是跨会话保证，也不等于 Userscript 合成事件能力。

最终只保留一个主路径和一个有确认条件的 fallback，不能同时触发多个动作。

为防止规则过宽或错误关联造成无限翻页，设置跳过熔断：

- 15 秒内最多连续跳过 12 条；
- 用户在一条允许视频上停留 3 秒后重置计数；
- 触发熔断后本页暂停自动跳过并提示用户检查规则；
- 弹幕过滤不受该熔断影响；
- 用户修改任一视频/BGM 配置后可解除熔断。

### 5.6 无感过滤契约

无感过滤由控制器内部的“逐卡决策门”统一完成，不为视频关键词、BGM 和导航建立三套互相竞争的门控。

每张标准视频卡片在每个配置 revision 和激活 epoch 中只能处于以下一种状态：

```text
pending -> allow
pending -> block -> navigating
pending -> bypass
```

- `pending`：尚未形成最终决策；媒体画面不可见，音频被临时抑制；
- `allow`：允许显示和播放；
- `block`：保持门控并交给统一导航流程，不允许先揭示再跳过；
- `navigating`：已经请求下一条，等待活动卡片改变；
- `bypass`：期限到达或发生不可恢复的不确定性，按 fail-open 显示和播放。

`allow`、`block` 和 `bypass` 对当前激活 epoch 都是终局状态。终局后到达的视频元数据或 BGM 元数据不得翻转当前状态；只有卡片离开后再次激活、Feed generation 变化或用户配置产生新的 revision，才创建新 epoch。已经进入 `navigating` 的旧 epoch 只允许完成或失败，不被新配置复用。

完整转换约束：

```text
pending -> allow | block | bypass
block -> navigating
navigating -> retired
allow | bypass -> retired
任意非 retired 状态 --根失效/页面隐藏/停止--> retired
```

不存在 `allow -> block`、`bypass -> block` 或旧 epoch 回到 `pending` 的转换。

#### 5.6.1 视频预判

1. Feed 新卡片进入 DOM 后，同步提取描述并创建决策记录；
2. 在卡片激活前，DOM 与已到达元数据都可以补充 `pending` 的输入快照；
3. 若任一已取得的视频文本字段命中，直接进入 `block`，不等待 BGM；
4. 卡片成为激活候选时，以当时可用的 DOM/元数据快照完成视频文本终判；此后迟到的视频文本不作用于本 epoch；
5. 若视频关键词未命中且 BGM 功能关闭，进入 `allow`；
6. 若 BGM 功能开启，则只按精确 `awemeId` 查询已缓存元数据；可判定时立即进入 `allow` 或 `block`；
7. 无元数据的离屏预加载卡片保持逻辑 `pending`，但不启动计时器；
8. 下一激活候选进入决策窗口时才应用目标最长 250ms 的可见门控；
9. 配置 revision 变化只重判尚未稳定激活的卡片；当前 `allow` / `bypass` 视频保持到离开，避免观看中途突袭跳过；
10. 新 revision 必须为受影响候选创建新的 epoch，旧 epoch 先标记 `retired`，禁止复用旧异步任务。

#### 5.6.2 视觉门控

- `document-start` 同步读取配置；只有视频或 BGM 规则实际生效时才安装视频门控 active class；
- CSS 只门控当前脚本识别为标准视频的媒体内容，不隐藏全站导航和控制栏；
- 卡片使用脚本专属决策属性，未知或特殊卡片应在同一任务内标记 `bypass`；
- 已知 `block` 卡片在导航确认前始终保持不可见；
- `allow` / `bypass` 必须一次性解除视觉门控和媒体门控；
- 门控失败、Observer 失败或 250ms watchdog 到期时必须 fail-open，不能让 Feed 长期空白。

允许内容在 BGM 数据缺失时可能看到目标不超过 250ms 的中性背景、上一稳定画面或未完成门控的视频帧。Stage 0 已观察到首帧早于标准活动卡片信号，因此只能对已可靠识别的预加载候选承诺提前门控；激活后才识别的卡片目标是避免进入稳定画面，不能声明绝对零帧。视觉承接和实际帧数转入 Stage 2/Stage 5 实页验收。

#### 5.6.3 音频门控

- Stage 0 只确认了媒体事件和绘制帧可观察，没有系统音频样本；不能从注册时机推导为已覆盖首次音频采样；
- 媒体写入是 Stage 2 独立安全闸门。只有下述 ownership、争用、恢复和下一卡不污染回归全部通过，才通过临时 `muted` 抑制 `pending` / `block` 音频；否则禁用媒体写入并降低音频承诺；
- 不调用 `pause()`，也不负责调用 `play()` 恢复；
- 每次写入创建内部 ownership token，记录 generation、配置 revision、激活 epoch、媒体元素身份、原始 `muted` 和脚本写入值；
- 门控期间监听目标媒体的 `volumechange` 及活动卡片变化。脚本自己的同步写入必须用 token 明确标记；任何无法归因于当前 token 的媒体变化都视为所有权争用；
- 无争用且 token、generation、epoch 和元素身份仍有效时，`allow` / `bypass` 才可把 `muted` 恢复为原值；
- 出现争用、元素被站点复用、根失效或属性状态不符合预期时，不根据“值仍相等”猜测所有权，不主动恢复该元素；立即撤销视觉门控、停止该 epoch 的自动跳过，并等待站点在新的活动卡片上建立原生媒体状态；
- Stage 2 源码回归和 Stage 5 人工验收必须验证保守不恢复路径不会污染下一张允许视频；若会污染，媒体元素原地修改方案不成立，首版禁用媒体写入并降低“零音频”承诺；
- 不写入抖音持久音量设置，不点击静音控件，不改变下一条允许视频的用户音量。

#### 5.6.4 弹幕首绘门控

- 弹幕规则生效时，为页面根启用脚本专属 `data-dwe-danmaku-active` 属性；
- `0.0.3` 修复将 CSS 门控限定为已拥有的 `pending/block` 节点；尚未具有脚本决策属性的 `[data-danmu-id]` 默认可见，避免新增元素遍历预算耗尽后永久隐藏；
- MutationObserver 在预算内同步提取文本并标记 `allow` 或 `block`；每批新增元素最多遍历 64 个，未遍历节点放行，后续文本或 ID 变化仍可重判；不承诺绝对零帧；
- 文本异步后填充的节点保持 `pending`，同时观察 `characterData`；
- 单条弹幕等待超过 100ms 或正文无法可靠提取时标记 `bypass`；
- Observer、根替换或控制器异常时先撤根属性，再清理专属属性，禁止让未决弹幕永久隐藏。

#### 5.6.5 连续命中

- 导航规划器可提前判定 DOM 中已经预加载的卡片，但不以其 DOM 前后顺序决定原生下一条是否可用；
- 连续的已知 `block` 卡片不得逐条揭示；
- 如果真实页面验证支持直接定位，直接导航到第一条已知 `allow` 卡片；
- 如果抖音必须逐条推进，则逐条请求原生切换，但所有中间 `block` 卡片继续保持视觉和音频门控；
- 遇到第一个未决卡片时停止向后猜测，进入该卡片的有界决策窗口；
- 原有“15 秒内最多连续跳过 12 条”熔断继续生效。

#### 5.6.6 无感验收阈值

首版以以下可观察指标作为分层验收目标，不把尚未取得的录屏或系统音频结果写成保证：

1. 对启用媒体门控的命中视频测量可听音频；目标为 0，未通过时禁用媒体写入并明确降级；
2. 已在进入视口前形成 `block` 的视频不出现可识别画面；
3. 激活时才命中的视频不得进入稳定画面，目标是不超过一个绘制帧；
4. 命中弹幕不出现“先显示后隐藏”；
5. 可见前台标签页中，BGM 未决卡片目标门控上限为 250ms，弹幕未决目标上限为 100ms；
6. 连续命中后落在第一条可可靠判定的允许内容；
7. 识别失败不会导致持续空白、静音残留、播放器停住或无法继续浏览。

“目标不超过一个绘制帧”必须通过真实浏览器录屏或逐帧检查验证，不能由 MutationObserver 代码路径推断为已通过；Stage 0 已确认现有活动卡片信号不足以保证这一目标。“零可听音频”也必须由系统音频采样确认。两项结果转入 Stage 2/Stage 5 验收，未通过时必须降低承诺，不能通过增加未经验证的 `pause()/play()` 补丁掩盖。

## 6. 模块与 seam

### 6.1 外部 interface

Userscript IIFE 内设置一个深模块：

```text
createDouyinEnhancerController({ runtime, logger })
  -> start(initialSettings)
  -> updateSettings(nextSettings)
  -> stop()
```

调用者只需要理解三种操作。控制器隐藏以下实现：

- 推荐路由判断；
- Feed 根发现和替换；
- 当前活动卡片选择；
- 视频文本提取；
- 逐卡决策门、配置 revision 和激活 epoch；
- 视觉门控，以及媒体安全闸门通过后才可启用的 ownership-token 临时音频抑制、争用检测及条件恢复；当前版本未启用媒体写入；
- 弹幕观察和所有权恢复；
- 页面内嵌数据解析；
- Fetch/XHR 透明观察；
- 视频与元数据关联；
- 切换确认、重试和熔断；
- Observer、timer 和 SPA 生命周期清理。

bootstrap 负责 GM 存储、菜单注册和初始设置读取。控制器不得直接调用 `GM_getValue`、`GM_setValue` 或注册菜单。

### 6.2 内部 runtime seam

生产和测试需要两个 Adapter，因此 runtime seam 是真实 seam。它按能力分组，不把十几个浏览器函数平铺到控制器 interface：

```text
runtime.document
runtime.location
runtime.observers
runtime.scheduler
runtime.routeSignals
runtime.navigation
runtime.pageTransport
runtime.mediaGate
```

- `observers` 创建 MutationObserver 和 IntersectionObserver；
- `scheduler` 提供时间、动画帧和计时器；
- `routeSignals` 合并 `popstate` 和 `pageshow`，并由轻量路由/根身份健康检查覆盖站点未派发标准事件的 SPA 变化；
- `navigation` 请求下一条并提供活动卡片变化确认；
- `pageTransport` 安装和卸载只读响应观察；
- `mediaGate` 是待验证的媒体能力 seam；当前生产实现不写 `muted`，也不调用 `pause()/play()`。

测试 Adapter 使用确定性时钟、假 DOM 和可控响应；生产 Adapter 使用浏览器对象。

### 6.3 首版不建立公开页面 Adapter

首版只有首页推荐流一种经过验证的页面实现，因此页面函数先作为控制器内部函数：

```text
isSupportedRoute()
findFeedRoot()
collectFeedCards(root)
findActiveVideoCard(root)
extractVideoText(card)
extractAwemeId(card)
findDanmakuRoot(card)
extractDanmakuText(node)
```

出现第二个已验证页面后，再把这些函数提取为页面 Adapter。提前抽象只会形成浅模块，并让未经验证的页面差异进入共享逻辑。

### 6.4 纯规则模块

```text
compileKeywords(rawValue) -> CompiledKeywords
matchesKeywords(text, compiledKeywords) -> boolean
evaluateVideo({ textFields, musicFields }, settings) -> Decision
```

`Decision` 只有 `allow`、`block`、`pendingMetadata` 三种结果，并携带类别，不携带完整用户词表。

### 6.5 元数据采集模块

首版元数据模块只为 BGM 过滤输出最小 DTO：

```text
{
  awemeId,
  musicTitle,
  musicName,
  relatedMusicTitle
}
```

不保留视频 URL、认证字段、Cookie、Token、完整响应或账号数据。

缓存为有上限的内存 LRU，建议最多 500 条；离开支持路由或控制器停止时清空，不写入 GM 存储。

## 7. BGM 数据采集设计

### 7.1 执行上下文

首版面向 Tampermonkey 5.x + Chromium，使用：

```text
@run-at document-start
@sandbox raw
```

Tampermonkey 官方文档将 `raw` 定义为请求页面 `MAIN_WORLD`，但 `document-start` 只表示尽可能早注入，不能据此保证早于页面保存原始 `fetch`、首帧或首次音频采样；`raw` 也可能因环境限制回退。Stage 0 因此分别采集执行 seam、传输和媒体证据，不把元数据注解本身当作保证。

Stage 0 在当前 Chrome + Tampermonkey 会话中确认：脚本于 `loading` 阶段启动，`GM_info` 与 `GM.info` 可用，`unsafeWindow` 可用但脚本 root 与页面 root 不同；通过 `unsafeWindow` 安装的包装观察到了页面 XHR 和 `fetch`。首个 XHR `open` 约在探针启动后 226ms，首个 `fetch` 调用约在 2622ms。

Stage 0 证明传输 seam 技术上可用，但 `1.0.7-test`、`1.0.8-test` 的单变量实页测试确认产品启用旧 BGM 传输路径会导致 Chrome 失去响应。离线最小回归进一步证明旧实现允许两个重叠 Feed 响应同时启动正文克隆，响应数量增长时没有背压上限。`1.0.10-test` 恢复传输 seam 并完成验证；此前 `0.0.2` 沿用该实现，全局只允许一个 fetch/XHR 正文处理任务，任务未结束时的新响应直接 fail-open，正文就绪后把 JSON 解析延后到独立任务。历史 script 同步全扫仍保持停用。

视频 DOM 过滤和弹幕过滤始终独立降级，不得静默把不可用的 BGM 或音频能力显示为可用。

### 7.2 首屏内嵌数据

历史调研确认页面首屏存在两类可利用数据：

- `createQuickPlayer({ awemeInfo: ... })` 中的当前视频对象；
- React/RSC 的 `recommendAwemeList` 序列化数据。

当前产品路径不扫描页面已有 script；以下解析原则只应用于运行期间由增量 Observer 新观察到的 QuickPlayer script：

- 只读取内联 script 文本；
- 禁止 `eval`、`Function` 或执行网页字符串；
- 使用明确前缀定位、JSON 字符串解码和带字符串状态的括号平衡扫描；
- 解析失败只影响 BGM 识别，不能影响页面播放；
- 使用匿名化真实片段建立 fixture；
- 页面格式变化时 fail-open。

### 7.3 后续 Feed 响应

`0.0.2` 沿用 `1.0.10-test` 重新接入并验证的以下受限产品路径：

- 原始 `fetch` / XHR 必须以原来的 `this` 和参数调用，并把原 Promise / Response 交还页面；
- `fetch` 元数据解析只读取克隆响应，不消费页面原响应；
- XHR 在 `loadend` 同步阶段只取得单飞资格并排入独立任务，随后只读 `responseText/response` 做有界解析；不修改 `responseType`、事件顺序或页面可见返回值；
- 只观察同源、成功、已实测为 Feed/Aweme 的 JSON 响应；
- 首个实测 allowlist 端点是 `GET /aweme/v1/web/tab/feed/`；新增端点必须先取得匿名真实网络证据；
- 全传输共享一个正文处理槽；一个 fetch clone 或 XHR 正文任务未完成时，后续候选响应不读取正文并直接 fail-open；运行令牌在停止或重启时使旧元数据失效，但正文槽保留到旧任务真正结束，避免 SPA 往返叠加 clone；
- `0.0.3` 修复把正文槽放到按页面对象索引的模块级 WeakMap，使该契约覆盖观察器被销毁后重建的路径；旧任务仍使用自身令牌释放槽，不向新运行投递元数据。本轮证据为 Node 回归，实页待验收；
- 对响应文本长度、DTO 遍历深度、节点数和 DTO 遍历时间设上限；`Response.text()` 的解码/分配和同步 `JSON.parse()` 无法被 JavaScript 墙钟截止抢占，因此 JSON 解析延后到独立任务，并通过单飞避免任务堆积；
- 任意读取、克隆、JSON 解析或字段提取错误都吞并到脚本自己的错误边界，绝不能改变原响应；
- 不修改请求、请求头、响应体、状态码或页面可观察的结果；克隆和异步解析仍可能增加 CPU、内存负担，因此“透明”只作为需测试的不改变语义目标，不作为零开销承诺；
- 不使用 `GM_xmlhttpRequest`，不声明 `@connect`。

Stage 0 曾确认后续 Feed 的主实测传输为 XHR，`0.2.1-test` 也验证了异步解析和条件恢复。`1.0.10-test` 的单飞回归证明重叠响应最多启动一个正文克隆；此前 `0.0.2` 沿用该边界以及后续取得的 Chrome + Tampermonkey SPA 与 15 分钟生命周期证据；`0.0.3` 的跨实例背压另由本轮 Node 回归覆盖，实页待验收。

### 7.4 数据提取

首版 BGM 字段提取支持经过 fixture 验证的 camelCase 和 snake_case 形态，例如：

- `awemeId` / `aweme_id`；
- `music.title`；
- `music.musicName` / `music.music_name`；
- `relatedMusicAnchor.extra.title` / `related_music_anchor.extra.title`，其中 `extra` 是有大小上限的 JSON 字符串。

递归遍历必须有深度和节点数上限。只接受同时具有视频标识或足够唯一文本字段的对象，不把任意名为 `title` 的站点配置误认为 BGM。

## 8. 页面生命周期

控制器状态：

```text
stopped
  -> discovering
  -> observing
  -> gating
  -> navigating
  -> suspended
  -> stopped
```

- `discovering`：支持路由内等待 Feed 根；
- `observing`：已连接 Feed、活动卡片和弹幕观察；
- `gating`：当前或下一候选卡片处于有界视觉/音频门控；
- `navigating`：命中后已请求下一条，等待活动卡片改变；
- `suspended`：离开支持路由或触发跳过熔断；
- `stop()`：彻底断开资源并恢复脚本拥有的 DOM 状态。

使用 generation 标识当前 Feed 根，使用 epoch 标识一次卡片激活决策。所有异步入口在读取数据后、产生副作用前都必须再次校验 generation、epoch 和 `retired` 状态；断开 Observer 不能替代该校验，因为已经排队的回调仍可能执行。

生命周期要求：

1. 文档启动后立即安装必要的首屏数据和传输观察；
2. DOM 可用后发现 `[data-e2e="slideList"]`；
3. 根出现后连接根观察、活动卡片选择和当前弹幕观察；
4. 新增卡片只登记候选，同步提取 DOM 字段并生成当前配置 revision 的决策，不对整个 Feed 重扫；
5. 活动候选进入视口前执行预判；可见前台中仍未决时启动不可重置的目标 250ms 卡片 watchdog；
6. 活动卡片变化时，先将上一 epoch 标记 `retired`，再取消 BGM 等待和导航确认，并按 ownership token 恢复或保守放弃媒体状态；
7. Feed 根替换固定按以下顺序执行：递增 generation 并将旧根/旧 epoch 标记 `retired` → 取消导航、timer、frame 和 BGM 等待 → 断开旧 Observer → 撤销旧根门控并恢复可安全恢复的媒体/弹幕状态 → 连接新根；
8. 离开支持路由或文档变为 `hidden` 时，先递增 generation/epoch 使旧任务失效，再撤销视觉和弹幕 active class、恢复或保守放弃媒体状态，最后断开 DOM 观察；不得在后台标签页维持待判定空白或临时静音；
9. 文档重新 `visible` 或返回支持路由时创建新的 generation/epoch 并重新发现，不复用旧 DOM 引用、旧决策或剩余倒计时；
10. `stop()` 使用与根替换相同的“先失效、后取消、再恢复”顺序，随后卸载传输包装并清空内存缓存。

根发现使用“MutationObserver 主路径 + 低频健康检查 fallback”。fallback 只检查路由、根连接状态和当前根身份，不扫描所有卡片。

## 9. 当前活动卡片判定

不能只依赖“正在播放的 video”，因为抖音会保留多个预加载播放器。稳定状态下的主判据固定为：

1. 唯一 `[data-e2e="slideList"][data-active="true"]` 作为当前 Feed 根；
2. 该根内唯一 `[data-e2e="feed-active-video"][data-e2e-vid]` 作为当前标准视频；
3. `data-e2e-vid` 必须通过 ID 形状校验，活动 ID 改变可作为导航完成确认；
4. 直播、无视频元素、推广卡片或任一唯一性条件失败时，本次判定 `bypass`，不得用几何或播放状态猜身份。

Chrome 基线中切换期间曾短暂出现活动 Feed 仍唯一、标准活动卡片为 0 的状态。控制器在该过渡窗口只等待有界确认或 fail-open，不选择相邻预加载卡片代替当前卡片。

IntersectionObserver、视口几何和媒体播放事件只提供候选发现或健康检查信号，不参与最终身份确认。首个视频帧早于上述活动卡片信号，因此这套判据也不能被表述为首帧前媒体门控信号。

## 10. 配置与菜单

建议存储键：

```text
douyinEnhancer.videoKeywordFilter.enabled
douyinEnhancer.videoKeywordFilter.keywords
douyinEnhancer.danmakuKeywordFilter.enabled
douyinEnhancer.danmakuKeywordFilter.keywords
douyinEnhancer.bgmKeywordFilter.enabled
douyinEnhancer.bgmKeywordFilter.keywords
```

每类两个菜单，共六个：

- `视频关键词屏蔽：未配置 / 已开启（N 个词） / 已关闭（已保存 N 个词）`
- `编辑视频屏蔽词（N 个）`
- `弹幕关键词屏蔽：...`
- `编辑弹幕屏蔽词（N 个）`
- `BGM 名称屏蔽：...`
- `编辑 BGM 屏蔽词（N 个）`

状态菜单行为：

- 未配置时点击直接打开编辑框；
- 已配置时点击切换开关；
- 编辑保存成功后立即更新控制器和菜单；
- 清空词表后回到“未配置”，运行态等同关闭；
- 持久化失败时保留旧运行态和旧菜单，不产生半更新。

菜单刷新复用稳定菜单 ID；如果当前 Tampermonkey 版本不支持可靠更新，则显式注销旧菜单再注册，测试确保不重复累积。

## 11. DOM 所有权与恢复

脚本允许写入的页面状态仅限：

- 视频卡片上的脚本专属决策属性；
- 弹幕节点上的脚本专属过滤属性；
- 必要的脚本专属 style 元素；
- 待判定媒体元素的临时 `muted` 状态及对应内存恢复记录；
- 可选的熔断/失败提示元素。

禁止：

- 删除、移动或重建抖音管理的 Feed、播放器和弹幕节点；
- 覆盖网站的 `style`、class、滚动高度或播放器设置；
- 通过网站音量按钮或持久设置实现临时音频门控；
- 把关键词、BGM 缓存或命中原因写入 DOM；
- 恢复不是脚本自己写入的隐藏状态。

提示 UI 若实现，应挂在 `document.documentElement` 下，使用独立前缀和 Shadow DOM 或严格作用域 CSS；默认自动消失，并支持 `prefers-reduced-motion`。

## 12. 故障策略

总体原则是 fail-open：增强功能失效时优先保持抖音原生可用。

| 故障 | 行为 |
| --- | --- |
| Feed 根或选择器变化 | 停止视频/BGM 过滤，弹幕若仍可定位则独立工作 |
| 当前卡片不确定 | 不跳过 |
| BGM 元数据缺失或关联不唯一 | 不跳过 |
| 可见前台 BGM 决策超过目标 250ms | 原子解除视觉/音频门控，对本激活周期标记 `bypass` |
| 可见前台弹幕正文超过目标 100ms 仍未就绪 | 对该弹幕标记 `bypass` 并显示 |
| 文档进入 `hidden` | 立即使当前 epoch 失效并 fail-open，取消门控计时；恢复可见后创建新 epoch |
| 视频门控或媒体状态恢复失败 | 立即撤销该卡片门控、停止自动跳过并限频提示 |
| 内嵌数据解析失败 | 记录一次限频错误，继续 DOM 功能 |
| Fetch 克隆或解析失败 | 原响应不受影响，停止处理该响应 |
| 下一条请求未确认 | 最多一次重试，随后放行并提示 |
| 连续跳过过多 | 触发熔断，暂停自动跳过 |
| Observer 构造失败 | 使用有界 fallback；仍失败则停用对应功能 |
| 配置存储失败 | 保持旧配置和旧运行态 |
| 控制器停止 | 恢复专属 DOM 状态并释放全部资源 |

错误日志默认不输出完整响应、视频 URL、账号标识或用户关键词。相同错误需要限频，避免控制台刷屏。

## 13. 性能预算

- 关键词只在配置变化时编译；
- 单次匹配复杂度为“字段文本长度 × 有效关键词数量”，首版不引入复杂索引；
- Feed MutationObserver 只收集新增/变化卡片；
- Feed 每批最多处理 64 条变更记录；截断时只遍历已拥有卡片检查连接及 Feed 归属，清理已移除卡片，不补扫整页或剩余变更。分类入口再次检查归属，避免同批旧记录重新接管已清理卡片；
- 弹幕 Observer 只处理新增、复用或文本变化的 `[data-danmu-id]`；
- 活动卡片重新判定按动画帧合并，每帧最多一次；
- 新卡片的 DOM 文本初判保持同步，避免已知命中内容进入下一次绘制；
- 可见前台中，视觉与音频门控的单卡目标期限为 250ms，不被 mutation、路由信号或重复元数据重置；实际回调可以因主线程繁忙而迟到，回调执行时必须先比较 `performance.now()` 截止时间再 fail-open；
- 弹幕决策同步完成；仅文本后填充在可见前台允许目标 100ms 等待；
- 后台标签页不承诺 250ms / 100ms 墙钟上限，也不维持门控；通过 Page Visibility 生命周期立即 fail-open；
- BGM 响应文本异步读取并在解析前按长度 fail-open；同步 `JSON.parse()` 后的 DTO 遍历有深度、节点数和 8ms 时间预算。浏览器原生文本解码和 `JSON.parse()` 不可由脚本抢占，因此不宣称整个解析过程有硬墙钟上限；
- 元数据缓存最多 500 条；
- 低频根健康检查建议 500ms～1000ms，只执行 O(1) 身份检查；
- 控制器停止后不得保留轮询、Observer 或响应解析任务。

实现阶段需要用 15 分钟连续浏览检查：

- 内存缓存是否有界；
- Observer 是否随根替换泄漏；
- 控制台错误是否限频；
- 响应克隆是否造成明显卡顿；
- 高频弹幕下是否出现长任务；
- 门控样式是否造成 layout shift、闪黑或连续空白；
- 若通过媒体安全闸门启用临时静音，其是否影响下一条允许视频的原始音量和播放状态。

## 14. 元数据与权限

预期 Userscript 头部只声明完成任务所需权限：

```text
@match https://www.douyin.com/*
@run-at document-start
@sandbox raw
@grant GM_getValue
@grant GM_setValue
@grant GM_registerMenuCommand
@grant GM_unregisterMenuCommand
@noframes
```

不声明：

- `GM_xmlhttpRequest`；
- `@connect`；
- Cookie、下载、剪贴板或通知权限；
- 页面历史、标签页或账号数据权限。

`@sandbox raw` 用于请求页面主世界执行，以便透明观察页面本来就会收到的响应。正式实现不申请 `window.onurlchange`；SPA 变化由页面事件和轻量健康检查共同识别。发布前仍应在目标 Tampermonkey 版本复核实际注入与清理行为。

## 15. 测试方案

项目测试使用 Node 18+ 内置 `node:test`，不引入 jsdom 或第三方依赖。测试通过控制器 interface 和内部 runtime seam 进行。

### 15.1 纯规则测试

- 中文、英文、Emoji；
- 大小写不敏感；
- NFKC 和连续空白规范化；
- 空项和重复项；
- 三组规则互不影响；
- 视频只匹配允许字段；
- BGM 只匹配名称字段。

### 15.2 元数据测试

- 匿名化 QuickPlayer fixture；
- 匿名化 RSC `recommendAwemeList` fixture；
- 匿名化后续 Feed JSON fixture；
- camelCase / snake_case；
- 畸形 JSON、截断脚本和未知结构 fail-open；
- 深度、节点数和响应大小上限；
- `awemeId/gid` 精确关联；
- 无 ID、ID 冲突和同 ID 不一致元数据时拒绝关联；
- 描述、作者、数组顺序和当前缓存唯一性都不能建立关联；
- DTO 不包含认证字段、视频 URL或完整响应。

### 15.3 控制器测试

- 启动时根尚未出现；
- 根动态出现和根替换；
- 活动卡片切换；
- 新卡片在进入视口前完成视频文本预判；
- `pending -> allow/block/bypass` 状态只能按合法方向变化；
- `allow/block/bypass` 在当前 epoch 都是终局，迟到视频/BGM 元数据不能翻转；
- 配置 revision 不突袭重判当前稳定视频，只为未激活候选和下一次激活创建新 epoch；
- 视频关键词命中只发起一次下一条；
- 视频关键词命中不等待 BGM；
- 视频命中后在导航确认前保持视觉和音频门控；
- BGM 元数据稍后到达后命中；
- BGM 250ms watchdog 不可被重复事件延长，超时后对当前激活周期放行；
- BGM `bypass` 后的迟到元数据不突袭跳过当前视频；
- 下一条未确认时最多重试一次；
- 根替换先递增 generation 和 retire 旧 epoch；Observer、watchdog、解析和导航的迟到回调不能操作新根；
- 连续跳过熔断和解除；
- 连续预判命中不会揭示中间卡片；
- 临时静音通过 ownership token 和 `volumechange` 争用检测决定是否恢复，不使用值相等推断所有权；
- 媒体争用、元素复用和保守不恢复路径不会污染下一条允许视频；
- 文档进入后台时立即 fail-open 并取消门控，恢复前台时创建新 epoch；
- 前台 watchdog 回调迟到时按绝对截止时间 fail-open，后台不承诺 250ms / 100ms；
- 门控异常按固定顺序 fail-open，不残留静音或不可见状态；
- 弹幕首批、动态新增、文本后填充和节点复用；
- 弹幕默认隐藏未决节点，100ms 后无法提取正文则 `bypass`；
- 关闭/清空词表恢复脚本拥有的弹幕；
- 三类配置独立更新；
- `stop()` 释放 Observer、timer、帧、响应包装和 DOM 状态。

### 15.4 传输透明性测试（停用实验模块）

- 包装后 `fetch` 的参数、`this`、返回 Promise 和 Response 语义保持不变；
- 解析 clone 不消费页面原响应；
- 包装后 XHR `open()` / `send()` 的参数、`this`、返回值、事件和响应语义保持不变；
- XHR `loadend` 只排入独立任务，不在页面事件栈内扫描响应；
- 两个重叠 Feed 响应最多启动一个正文克隆或读取任务；
- `stop()` 移除尚未触发的 XHR 监听器，不让迟到响应继续产生探针或正式脚本副作用；
- 解析异常不改变请求结果；
- 非同源、非 allowlist、非 JSON 响应不克隆；
- 卸载后恢复原始函数，且只恢复脚本自己的包装；
- 若发现函数已被其他脚本再次包装，不覆盖后来者；
- 实测页面预先缓存原始 `fetch` 时，脚本能检测并停用后续响应观察，不错误声明已覆盖；
- 记录克隆解析对微任务、CPU 和内存的增量，不把“透明”解释为零开销。

这些测试证明产品传输模块的调用语义与单飞边界；`0.0.2` 继承的 `1.0.10-test` 已完成仅 BGM、SPA 往返和 15 分钟定向生命周期回归，但仍需独立性能 trace。

### 15.5 菜单测试

- 未配置、开启、关闭三种状态；
- 有效词数来自规范化结果；
- 菜单不显示原始关键词；
- 保存失败不产生半更新；
- 刷新菜单不重复注册；
- 六个存储键互不串用。

## 16. 真实浏览器验收

浏览器验收必须在 Tampermonkey 中保存当前最新版脚本后执行，并区分源码测试与实页证据。自动跳过和媒体写入只在对应 Stage 2 能力闸门通过并启用后执行验收；闸门失败时，验收结果应记录为“能力已禁用/已降级”，不能把步骤不适用写成通过。

### 16.1 视频关键词

1. 使用当前视频描述中的临时唯一词；
2. 刷新首屏后命中并只切换一条；
3. 下滑后新加载视频继续生效；
4. 未命中视频可正常播放；
5. 关闭、清空和重新开启后立即符合菜单状态；
6. 连续命中触发熔断，不无限翻页；
7. 直播、广告或不支持卡片不被误处理。
8. 使用录屏逐帧检查：提前命中的视频不出现可识别画面，激活时命中的视频不进入稳定画面；
9. 若媒体写入已启用，使用系统或浏览器音频监测测量是否存在可听音频；目标为 0，失败则禁用该能力；
10. 允许视频的音量、静音和播放状态不被上一条门控污染。

### 16.2 弹幕关键词

1. 使用当前弹幕中临时唯一词；
2. 已显示和后续新增弹幕均被隐藏；
3. 其他弹幕保持原动画和交互；
4. 节点复用后新文本重新判断；
5. 关闭或清空后只恢复脚本隐藏的弹幕；
6. 高频弹幕下无明显卡顿；
7. 使用录屏逐帧检查命中弹幕不会先显示再隐藏；
8. 构造文本后填充节点，确认最长 100ms 后 fail-open，且不会永久隐藏。

### 16.3 BGM 名称

1. 从当前视频对象确认真实 `awemeId`、原声 `music.title/musicName` 和平台关联歌曲 `relatedMusicAnchor.extra.title`；
2. 使用唯一名称词后当前或后续视频正确跳过；
3. 首屏内嵌数据和无限滚动响应各覆盖一次；
4. 卡片无 `awemeId/gid` 时必须 fail-open，即使描述 + 作者在当前缓存中唯一；
5. 制造重复描述、ID 冲突和同 ID 不一致元数据，确认均不会误跳过；
6. 禁止把相邻视频 BGM 错配到当前视频；
7. Network 面板确认脚本没有新增请求；
8. 对缓存命中场景确认视频在进入视口前完成判定；
9. 对可见前台的元数据延迟场景测量目标门控是否不超过 250ms；若媒体写入已启用，同时测量音频泄漏，失败则禁用该能力；
10. 对元数据超过期限场景确认视频正常放行，迟到元数据不会在观看中途突然跳过。

### 16.4 生命周期与兼容性

1. 硬刷新、SPA 往返和浏览器后退；
2. Feed 根替换和预加载卡片回收；
3. Edge + Tampermonkey；
4. Chrome + Tampermonkey；
5. 连续浏览 15 分钟；
6. 页面原生连播、音量、暂停和弹幕开关不被持久修改；
7. 连续配置 3～5 个可命中视频，确认中间卡片不闪现并落在第一条允许内容；
8. 让 Feed 根在 `pending` 和 `navigating` 期间分别替换，确认不会残留空白、静音或旧任务误操作新根；
9. 在 BGM `pending`、弹幕 `pending` 和导航期间切到后台，确认立即 fail-open；返回前台后使用新 epoch，旧 timer 不生效；
10. 门控期间手动或由网站改变静音状态，确认争用检测不会覆盖后来选择，也不会污染下一条允许视频。

验收报告必须分别列出：

- 源码语法检查；
- Node 单元/控制器测试；
- 当前浏览器真实 DOM 结果；
- 当前浏览器网络与性能结果；
- 尚未验证的浏览器和页面。

## 17. 实施阶段

### Stage 0：证据固化

Stage 0 证据采集已收敛。已确认：

- 活动 Feed/卡片、三卡窗口、视频文本和弹幕所有权边界；
- QuickPlayer 与后续 XHR Feed 的 BGM 字段形态，以及 ID-only 关联所需的同对象关系；
- Chrome + Tampermonkey 中 GM API、`unsafeWindow`、XHR/fetch 页面 seam 可以共存；
- 媒体事件、首帧、预加载视频上下文和弹幕结构可由只读探针观察；
- 普通推荐路由对探针完全惰性，显式捕获不写 DOM、媒体或导航，也不发送额外请求。

同时确认了两个限制：现有活动卡片信号晚于首帧；浏览器控制键盘输入跨浏览器/焦点不稳定，不能充当 Userscript 可编程导航证据。

以下项目不再继续扩大 Stage 0 探针，而是按责任迁移：

- 可编程下一条：Stage 2 能力闸门；失败则不实现自动跳过，降级为可见提示；
- 临时 `muted`、争用与恢复：Stage 2 源码回归 + Stage 5 人工验收；失败则禁用媒体写入；
- 弹幕后填充与节点复用：Stage 3 防御性实现、Node 回归和实页验收；
- 后台切换：控制器回归和 Stage 5 人工验收；
- 系统音频、250ms/100ms 和高帧率结果：对应功能阶段测量并在 Stage 5 分层报告，不再阻塞 Stage 1。

匿名证据位于 `test/fixtures/`，完整判定见 `docs/2026-08-13-stage-0-evidence.md`。仓库当前探针 `0.2.1-test` 已通过 17 项离线回归和 Chrome + Tampermonkey 窄范围冒烟；它本身不是产品脚本，正式实现仍需独立实页回归。

### Stage 1：项目骨架和规则配置

- 已完成单文件元数据；
- 已完成六个 GM 存储键和动态菜单；
- 已完成纯关键词模块；
- 已完成 Node 测试和 README 基础说明；
- 已在 Chrome + Tampermonkey 中验收六个菜单、规范化计数、开关、刷新持久化、清空恢复和普通页面惰性；完整证据见 `docs/2026-08-13-stage-1-evidence.md`。

### Stage 2：视频关键词与导航

- Feed 生命周期；
- 活动卡片判定；
- 视频字段提取；
- 逐卡决策门、终局 epoch、可见前台视觉门控和目标 250ms watchdog；
- 可编程下一条能力闸门已在 Chrome + Tampermonkey 中 3/3 通过；下一条确认、一次重试和熔断已在 `0.2.1-test` 实现；Edge 当前推荐流缺少同等语义控件，因此按能力闸门 fail-open；
- 先通过 ownership-token、争用、清理和下一卡不污染回归，再启用临时静音；闸门失败时禁用媒体写入；
- 连续命中规划；
- 控制器回归测试、逐帧录屏和推荐流实页验收。

### Stage 3：弹幕过滤

- 弹幕根和正文 Adapter；
- 增量观察、首绘门控、100ms watchdog、节点复用和恢复；
- 高频弹幕测试及逐帧实页验收。

### Stage 4：BGM 元数据（当前降级）

- 历史版本完成 QuickPlayer 首屏静态解析与 `GET /aweme/v1/web/tab/feed/` 的 XHR/fetch 兼容观察；`1.0.9-test` 曾临时停用传输来源，`1.0.10-test` 以全传输单飞背压恢复，此前 `0.0.2` 沿用该受限路径，`0.0.3` 补齐跨实例背压；
- 当前解析运行期间新增的 QuickPlayer script，并以单飞方式观察页面原本收到的 Feed 响应；不执行页面源码，不扫描页面已有 script；
- 已完成最小 DTO（ID、原声两个名称、关联歌曲名称）、有界缓存、字符串 ID 精确关联和冲突 fail-open；
- 已完成预加载判定、迟到元数据 `bypass` 语义，并与视频关键词共用单一导航状态机；
- 传输透明性与单飞测试验证当前受限传输路径的调用语义和背压边界；真实 BGM 命中、仅 BGM、多模块组合及生命周期实页回归共同构成功能证据。

### Stage 5：交付

- 已完成禁用脚本、规则全关和仅视频模块的 SPA 对照；历史仅 BGM 阻塞已在 `1.0.10-test` 修复并完成定向回归；
- `1.0.7-test` 已取消运行中启用 BGM 时同步扫描全部历史 script，保留首次启动兼容扫描、新增 script 增量处理和 Feed 网络观察；基线恢复但 SPA 返回后仍卡死；
- `1.0.8-test` 将 BGM DTO 子项枚举纳入节点预算，单个高扇出数组或对象不再先读取全部子项，超出即 fail-open，待同场景实页复测；
- `1.0.8-test` 人工实测仍出现整页卡死；`1.0.9-test` 临时从产品路径移除 BGM fetch/XHR 包装和历史 script 扫描，造成覆盖率下降；`1.0.10-test` 用重叠响应红灯锁定无背压正文克隆，并以全传输单飞、延后解析恢复 Feed 数据源；真实 BGM 命中、仅 BGM、三模块组合以及约 15.6 分钟推荐流和 3 次 SPA 往返均通过；正式版本序列以相同代码从 `0.0.1` 开始记录；
- 已完成 `0.4.9-test` Chrome + Tampermonkey 功能实页验收并复现渲染进程无响应；`0.4.10-test` 完成 78 次连续上下切换、15 秒空闲恢复和 BGM 菜单弹框回归，未复现失去响应；Edge 已确认导航控件缺失时的 fail-open 降级，弹幕端到端仍未验证；
- 已完成 README 安装、更新、配置、停用、卸载、限制和隐私说明；
- 已完成发布候选脱敏扫描和 Stage 5 分层证据报告；
- 约 15.6 分钟连续浏览和 3 次关键 SPA 往返已经闭环；Network 面板、独立性能采样、真实根异常替换和后台场景尚未闭环，因此不标记正式发布完成；
- 只在用户明确要求时提交、推送或发布。

## 18. 风险排序

| 风险 | 等级 | 应对 |
| --- | ---: | --- |
| BGM 元数据与当前卡片错配 | 高 | 仅允许 `awemeId/gid` 精确关联；无 ID、冲突或不一致均放行 |
| 自动下一条重复触发或跳两条 | 高 | 单控制器、确认前互斥、一次 fallback、先递增 generation/retire epoch，再清理旧任务 |
| 临时静音覆盖用户/网站选择 | 高 | ownership token、`volumechange` 争用检测、争用时保守放弃恢复并验证下一卡状态 |
| BGM 传输再次引发主线程或内存压力 | 高 | 全传输单飞、重叠响应 fail-open、解析延后；仅 BGM 与 SPA 往返已通过，正式发布前仍需独立性能 trace |
| 页面响应格式变化 | 中高 | fixture、allowlist、安全解析、失败仅降级 BGM |
| 混淆 class 变化 | 中 | 优先 `data-e2e` / `data-danmu-id`，易变细节集中在内部页面函数 |
| 规则过宽导致翻页风暴 | 中 | 连续跳过熔断、允许视频停留后重置 |
| 响应克隆造成性能损耗 | 中 | 最多一个进行中的正文克隆；长度和 DTO 预算继续生效，真实开销用独立性能 trace 验证 |
| 弹幕节点复用导致误隐藏 | 中 | 观察文本变化、每次复用重判、专属所有权恢复 |
| 浏览器页面导航能力差异 | 中 | Chrome + Tampermonkey 为当前完整功能验证环境；Edge 缺少语义化导航控件时明示并 fail-open，不使用合成键盘/滚轮伪装可信输入 |
| 后台 timer 节流延长门控 | 中 | `visibilitychange` 时立即 fail-open；目标时限仅适用于可见前台 |

## 19. 最终建议

可以做成油猴脚本，且视频关键词和弹幕关键词适合直接采用 DOM 增强实现。BGM 名称过滤也可行，但它不是纯 DOM 功能，应被设计为可独立降级的数据增强层：只有在取得并可靠关联页面已接收的元数据时才跳过，否则放行。

首版不应追求“一次覆盖抖音所有页面”。先把推荐流、下一条互斥确认、弹幕节点复用和 BGM 关联做成可测试的深模块，能显著降低抖音前端变化时的维护范围。

Stage 0 与 Stage 1 均已结束。Stage 2 可编程导航能力闸门已在 Chrome + Tampermonkey 中用探针取得 3/3 实页证据；正式视频控制器已完成真实输入方向识别、动画锁释放后导航及连续命中事务，`0.4.8-test` 由用户确认连续命中恢复正常。Edge + Tampermonkey 当前推荐流未暴露同等语义导航控件，视频/BGM 命中后按契约 fail-open；合成键盘、滚轮和 `scrollIntoView()` 都不是可靠替代。Stage 3 弹幕控制器 `0.3.1-test` 已完成源码、Node 回归及推荐页/独立视频页实页验收，但未单独完成 Edge 端到端验收。Stage 4 已修复顶层字段定位并同时覆盖原声和平台关联歌曲，用户确认 BGM 自动跳过生效。系统音频、高帧率零帧和媒体写入能力闸门仍未通过，因此本版不写媒体状态，也不承诺绝对零音频或零帧。

## 20. 对抗式审查意见处置

2026-08-13 的独立审查结论为“可进入纯证据采集 Stage 0，但需先修订才能进入功能编码”。本方案按以下方式处置：

| 审查意见 | 处置 | 方案变化 |
| --- | --- | --- |
| 描述 + 作者的缓存唯一性不能证明视频身份 | 采纳 | 首版 BGM 只允许 `awemeId/gid` 精确关联，无 ID 一律放行 |
| 用属性值相等判断媒体所有权不成立 | 调整采纳 | 改为 ownership token + `volumechange` 争用检测 + 条件恢复；争用时不猜测所有权，并把“不污染下一卡”设为 Stage 2 媒体安全闸门 |
| 根替换时先恢复、后递增 generation 存在迟到任务竞态 | 采纳 | 固定为先递增 generation/retire epoch，再取消、断开和恢复；每个异步副作用前复核身份 |
| `allow` 终局、迟到元数据和配置立即生效语义冲突 | 采纳 | 补完整状态转换；`allow/block/bypass` 对当前 epoch 终局；视频/BGM 配置不突袭跳过当前稳定视频 |
| `document-start + raw` 不能直接推出早于 fetch/首帧/首音频 | 采纳 | Stage 0 分别记录传输和媒体时序；确认 XHR seam 可用但活动卡片晚于首帧，后续按能力分别验收和降级 |
| 后台标签页 timer 会被节流 | 采纳 | 250ms/100ms 仅定义为可见前台目标；进入后台立即 fail-open，返回后新建 epoch |
| `pause()` 后 `play()` 可能受自动播放策略限制 | 采纳 | 首版默认只使用临时 `muted`；`pause()/play()` 不作为实现或补救路径 |

没有采纳“只要争用就永不恢复任何媒体状态”的字面实现，因为它可能把脚本静音残留到站点复用的下一张卡片。替代方案是把争用检测、条件恢复和下一卡不受污染共同列为 Stage 2 媒体安全闸门；若无法同时满足，则不采用直接修改媒体元素的音频门控方案，并降低“零可听音频”承诺。
